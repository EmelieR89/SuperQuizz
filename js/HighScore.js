// Denna klassen håller koll på vem som vann och hur många gånger de har vunnit
class HighScore {
    constructor () {
        // this.wins = amountwins
        // this.guesses = amountguesses
        
        // this.bot1amount
        // this.bot2amount
        // this.bot3amount
    }

    // getScore() number {
    //     this.score = Math.round(((4 * Math.log(this.wins)) / 1.6 ) + ((this.wins) - (this.bot1amount * 0.3) - (this.bot2amount * 0.2) - (this.bot3amount * 0.1) / (this.amountguesses) * 100
    //     )
    // }
}